import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div>

      <h1 className="text-2xl font-bold text-center p-3">Pagina Home</h1>
      <Button>Home</Button>
    </div>
  )
};

export default Home;