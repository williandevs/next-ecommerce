import { Button } from "@/components/ui/button";

const Product = () => {
  return (
    <div>
      Product
      <Button className="p-3 text-orange-500 w-full rounded-full bg-red-900">
        Click me
      </Button>
    </div>
  );
};

export default Product;
